/** @format */

let aumentanumeri = 0;

document.getElementById('clicks');

document.addEventListener('clicks', function () {
  aumentanumeri = aumentanumeri + 1;
  // in forma contratta aumentanumeri ++(è uno), -- diminuisce di uno
});
